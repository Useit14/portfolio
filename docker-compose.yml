version: "3.8"

services:
  ctlog:
    build:
      context: ./apps/ctlog
    command: npm run start
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
#   project2:
#     build:
#       context: ./apps/project2
#     ports:
#       - "3001:3001"
#     environment:
#       - NODE_ENV=production
